{% extends 'base.html.twig' %}

{% block title %}WebsiteHandler{% endblock %}

{% block body %}
    <h1>WebsiteHandler</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ website_handler.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ website_handler.label }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ website_handler.url }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ website_handler.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('websitehandler_index') }}">back to list</a>

    <a href="{{ path('websitehandler_edit', {'id': website_handler.id}) }}">edit</a>

    {{ include('website_handler/_delete_form.html.twig') }}
{% endblock %}
